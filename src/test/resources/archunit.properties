# This service has no inbound/driving adapters yet (background CalDAV poller,
# no REST/UI entry point) — the driving-port/-adapter rules would otherwise
# fail on "no classes matched" before any such class exists.
archRule.failOnEmptyShould=false
