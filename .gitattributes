# Shell scripts must keep LF line endings regardless of a clone's core.autocrlf setting --
# a CRLF shebang line breaks execution on Linux ("bad interpreter") where build-and-export-
# image.sh is actually meant to run.
*.sh text eol=lf
